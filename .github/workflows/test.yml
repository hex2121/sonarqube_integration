name: DemoDeployment_Sonar

on:
  pull_request:
    branches: ["main"]

jobs:
  unit-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # important for Sonar blame info later

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.13

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Unit Tests with Coverage
        run: |
          python -m pytest test.py -s -v --maxfail=1 --disable-warnings --junitxml=results.xml

      - name: Upload coverage for next job
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: test-coverage.xml


  # -----------------------------
  # Job 2: Run SonarQube Code Analysis
  # -----------------------------

#  sonar-analysis:
#    name: Run SonarQube Code Analysis
#    runs-on: ubuntu-latest
#    needs: unit-tests  # ensures tests run before analysis
#
#    steps:
#      - name: Checkout Repository
#        uses: actions/checkout@v4
#        with:
#          fetch-depth: 0
#
#      - name: Download coverage from previous job
#        uses: actions/download-artifact@v4
#        with:
#          name: coverage-report
#          path: .
#
#      - name: Set up JDK (required by SonarScanner)
#        uses: actions/setup-java@v4
#        with:
#          java-version: '17'
#          distribution: 'temurin'
#
#      - name: Start SonarQube (local instance)
#        run: |
#          docker run -d --name sonarqube -p 9000:9000 sonarqube:lts-community
#          echo "⏳ Waiting for SonarQube to be ready..."
#          for i in {1..40}; do
#            STATUS=$(curl -s http://localhost:9000/api/system/status | jq -r '.status')
#            if [ "$STATUS" = "UP" ]; then
#              echo "✅ SonarQube is up!"
#              break
#            fi
#            echo "Waiting... ($i)"
#            sleep 5
#          done
#          docker ps
#
#      - name: Install SonarScanner
#        run: |
#          echo "⬇️ Downloading SonarScanner CLI..."
#          curl -sSLo sonar-scanner.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-6.2.1.4610-linux-x64.zip
#          unzip sonar-scanner.zip
#          mv sonar-scanner-* sonar-scanner
#          chmod +x sonar-scanner/bin/sonar-scanner
#
#      - name: Generate SonarQube Token
#        run: |
#          echo "🔑 Generating auth token..."
#          sleep 10
#          TOKEN=$(curl -u admin:admin -X POST "http://localhost:9000/api/user_tokens/generate?name=ci-token" | jq -r '.token')
#          echo "SONAR_TOKEN=$TOKEN" >> $GITHUB_ENV
#
#      - name: Run SonarQube Analysis
#        env:
#          SONAR_HOST_URL: http://localhost:9000
#          SONAR_PROJECT_KEY: demo-python
#          SONAR_TOKEN: ${{ env.SONAR_TOKEN }}
#        run: |
#          echo "🔍 Running SonarScanner Analysis..."
#          ./sonar-scanner/bin/sonar-scanner \
#            -Dsonar.projectKey=$SONAR_PROJECT_KEY \
#            -Dsonar.sources=. \
#            -Dsonar.host.url=$SONAR_HOST_URL \
#            -Dsonar.login=$SONAR_TOKEN \
#            -Dsonar.projectName="Demo Python Project" \
#            -Dsonar.projectVersion=1.0 \
#            -Dsonar.python.version=3.13 \
#            -Dsonar.python.coverage.reportPaths=test-coverage.xml \
#            -Dsonar.qualitygate.wait=true \
#            -Dsonar.exclusions=sonar-scanner/**,**/*.zip \
#            -Dsonar.verbose=true
